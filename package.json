{
  "name": "fairdrive",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd fairdrive-frontend && npm install && npm run build",
    "vercel-build": "cd fairdrive-frontend && npm install && npm run build"
  }
}